import { Routes } from '@angular/router';
import { FormFeatureComponent } from './form/feature/form-feature/form-feature.component';

export const routes: Routes = [
  {
    path: '', component: FormFeatureComponent
  }
];
